# Archivos fuente / internos — no deben servirse públicamente en onepack.com.sv
# El sitio solo usa la carpeta assets/ ; estas son duplicados o material de origen.
# Leading / anchors each pattern to the deployment root to prevent accidental depth-matching.
/AGENTS/
/Branding/
/Business/
/Typography/
/Imgs/
/Logos-web*/
**/.DS_Store
